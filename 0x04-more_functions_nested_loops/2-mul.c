#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply 2 integers.
 * @a: integer multiplied with b
 * @b : integer multipled with a
 * Return: result
 */

int mul(int a, int b)
{
	return (a * b);
}
